<!--this contains the classifications details view for the
cars users look for.-->
<% if (title) { %>
    <%# messages() %>
    <h1><%= title%> </h1>
    <%- grid %>
<% } else {
    res.redirect('/')
} %>